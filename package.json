{
  "name": "web-systems-lecture",
  "version": "1.0.0",
  "scripts": {
    "test": "export NODE_ENV=test && mocha --reporter spec --recursive"
  },
  "author": "Eric Anderson",
  "license": "ISC",
  "dependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "nedb": "^1.8.0"
  }
}
